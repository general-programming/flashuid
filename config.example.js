const config = {
    shortUID: "69696969",                   // currently we only support 4 byte UIDs
    flashFullSectorZeroBlockZero: false,    // we also support full Sector 0 Block 0 flashing
    fullSectorZeroBlockZero: ""             // so enter the block here and set the above setting true!
};

module.exports = config;